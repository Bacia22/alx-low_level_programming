#include <stdio.h>

/**
* main - prints the name of a programe
* @orgc: array size
* @argv: array containing elements
*
* Return: 0 on success
*/
int main(int orgc, char **orgv)
{
puts(orgv[orgc - 1])
return (0);
}
