#include "main.h"

/**
* main - Test function for positive or negative
* Return: Always 0
*/
int main(void)
{
int i;
i = 0;
if (i == 0)
{
printf("%d is zero", i);
return (0);
}
positive_or_negative(i);
return (0);
}
